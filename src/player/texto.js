export default class TextManager {
    // Método estático para exibir todo o texto de uma vez
    static textoDialogo(scene, text, textObject) {
        // Define o texto completo no objeto de texto
        textObject.text = text;
    }
}
